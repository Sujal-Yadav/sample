import React from "react";
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  SafeAreaView,
} from "react-native";

const SurveyPage = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Hello this is the survey page</Text>
      </View>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default SurveyPage;
